---
title: 基于Qt的插件
parent: 插件开发
nav_order: 5.5
---

## 基于Qt的插件

插件必须实现如下接口(C语言接口)：

```cpp
int xFlowInPortsCount();
int xFlowOutPortsCount();
QString xFlowInPortType(int index);
QString xFlowOutPortType(int index);
QWidget* xFlowParametersWidget(int index, QWidget* parent);
```
